apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.db.service.name }}
  labels:
    app: {{ .Values.db.name }}
spec:
  type: {{ .Values.db.service.type }}
  selector:
    app: {{ .Values.db.name }}
  ports:
    - port: {{ .Values.db.port }}
      targetPort: vprodb-port
      protocol: TCP
