apiVersion: v1
kind: Service
metadata:
  name: vproapp-service
  labels:
    app: vproapp
spec:
  type: ClusterIP
  selector:
    app: vproapp
  ports:
    - port: 8080
      targetPort: vproapp-port
      protocol: TCP